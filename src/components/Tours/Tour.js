import React from 'react';
import SingleTour from './SingleTour'

const Tour = () => {
    return (
        <div>
            this is tour component
            <SingleTour/>
        </div>
    );
};

export default Tour;