import React from 'react';
import SingleTour from './SingleTour'

const Tours = () => {
    return (
        <div>
            this is tour component
            <SingleTour/>
        </div>
    );
};

export default Tours;